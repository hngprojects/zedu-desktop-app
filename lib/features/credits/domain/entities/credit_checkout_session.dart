class CreditCheckoutSession {
  const CreditCheckoutSession({
    required this.checkoutSessionId,
    required this.checkoutSessionUrl,
  });

  final String checkoutSessionId;
  final String checkoutSessionUrl;
}
